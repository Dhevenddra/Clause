# Required — from the hackathon Fireworks credit allocation
FIREWORKS_API_KEY=your-fireworks-api-key-here

# Inference endpoint (DEC-003). Default = Fireworks.
# To run on the MI300X pod: point this at your vLLM endpoint, e.g. http://<pod-ip>:8000/v1
OPENAI_BASE_URL=https://api.fireworks.ai/inference/v1

# Verify the exact Gemma model ID with: python scripts/smoke_test.py --list
MODEL_ID=accounts/fireworks/models/gemma-4-31b-it
